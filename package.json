{
  "name": "image-shield",
  "version": "0.9.0",
  "description": "Image fragmentation and restoration.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "workspaces": [
    "packages/*"
  ],
  "files": [
    "dist",
    "packages/*/dist"
  ],
  "scripts": {
    "build": "npm run build:packages && tsc -p tsconfig.build.json",
    "build:packages": "npm run build -w @image-shield/core && npm run build -w @image-shield/node && npm run build -w @image-shield/cli && npm run build -w @image-shield/browser",
    "build:check": "npm run build:check:packages && tsc -p tsconfig.build.json --noEmit",
    "build:check:packages": "npm run build:check -w @image-shield/core && npm run build:check -w @image-shield/node && npm run build:check -w @image-shield/cli && npm run build:check -w @image-shield/browser",
    "build:typecheck": "npm run build:check:packages && tsc -p tsconfig.build.json --noEmit",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "prepare": "husky",
    "test": "npm run test --workspaces",
    "test:ci": "npm run test --workspaces",
    "test:coverage": "vitest --coverage"
  },
  "keywords": [],
  "author": "tuki0918",
  "repository": {
    "type": "git",
    "url": "https://github.com/tuki0918/image-shield.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.10.2",
    "@types/pngjs": "^6.0.5",
    "@types/semver": "^7.7.1",
    "@vitest/coverage-v8": "^3.1.4",
    "husky": "^9.1.7",
    "typescript": "^5.7.2",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.1.4"
  },
  "dependencies": {
    "@image-shield/core": "file:packages/core",
    "@image-shield/node": "file:packages/node",
    "@tuki0918/seeded-shuffle": "^1.0.0",
    "jimp": "^1.6.0",
    "semver": "^7.7.3"
  }
}
