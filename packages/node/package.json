{
  "name": "@image-shield/node",
  "version": "0.9.0",
  "description": "Node.js implementation of image fragmentation and restoration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:check": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@image-shield/core": "file:../core",
    "@tuki0918/seeded-shuffle": "^1.0.0",
    "jimp": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pngjs": "^6.0.5",
    "typescript": "^5.7.2",
    "vitest": "^3.1.4"
  }
}
