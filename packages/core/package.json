{
  "name": "@image-shield/core",
  "version": "0.9.0",
  "description": "Core image fragmentation and restoration logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:check": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@tuki0918/seeded-shuffle": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^3.1.4"
  }
}
